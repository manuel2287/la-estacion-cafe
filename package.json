{
  "name": "dsw-tp2-la-estacion",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:css": "sass scss/styles.scss css/styles.css --style=compressed",
    "watch:css": "sass --watch scss/styles.scss:css/styles.css",
    "serve": "live-server --open=index.html --port=5500",
    "serve:host": "live-server --host=0.0.0.0 --open=index.html --port=5500"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "sass": "^1.101.0"
  }
}
